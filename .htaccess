# RewriteEngine On
# RewriteCond %{HTTPS} off
# RewriteRule (.*) https://%{HTTP_HOST}%{REQUEST_URI} [R=301,L]


# RewriteEngine On
# RewriteCond %{SERVER_PORT} 80
# RewriteRule ^(.*)$ https://www.bizzeonline.co/$1 [R,L]

# RewriteEngine on

# # force ssl
# RewriteCond     %{SERVER_PORT} ^80$
# RewriteRule     ^(.*)$ https://%{SERVER_NAME}%{REQUEST_URI} [L,R]